spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/Shiro?useUnicode=true&useSSL=false&characterEncoding=utf8&serverTimezone=UTC
    username: root
    password: lanxin

  redis:
    port: 6379
    host: localhost

server:
  port: 8889


mybatis-plus:
  mapper-locations: classpath:mappers/*.xml
  config-location: classpath:mybatis.xml